--- a/config.schema.json	2026-08-06 19:40:06
+++ b/config.schema.json	2026-08-06 19:40:06
@@ -69,7 +69,25 @@
         "type": "integer",
         "required": false,
         "default": 100000000,
-        "description": "Maximum number of bytes FFmpeg will read while probing the stream to detect its parameters. FFmpeg stops as soon as it has detected the stream, so this is an upper bound, not a fixed amount. Defaults to 100000000 (100 MB), the plugin's long-standing value. WARNING: same caveat as Analyze Duration — setting it too low can cause a camera to fail to start with 'dimensions not set' if its H.264 parameter sets haven't arrived yet. Raise it (or keep the default) if a camera fails to start."
+        "description": "Maximum number of bytes FFmpeg will read while probing the stream to detect its parameters. FFmpeg stops as soon as it has detected the stream, so this is an upper bound, not a fixed amount. Defaults to 100000000 (100 MB), the plugin's long-standing value. WARNING: same caveat as Analyze Duration \u2014 setting it too low can cause a camera to fail to start with 'dimensions not set' if its H.264 parameter sets haven't arrived yet. Raise it (or keep the default) if a camera fails to start."
+      },
+      "hksvPrebufferSeconds": {
+        "title": "HKSV Prebuffer: Pre-roll (seconds)",
+        "type": "integer",
+        "required": false,
+        "default": 0,
+        "minimum": 0,
+        "maximum": 15,
+        "description": "Seconds of footage from BEFORE the trigger to prepend to an HKSV recording. 0 disables it. Requires the local go2rtc RTSP layer from the install guide. This value is also advertised to HomeKit as prebufferLength, and the hub trims the stored clip to it, so it bounds what is actually kept. Must not exceed the retention below."
+      },
+      "hksvPrebufferRetainSeconds": {
+        "title": "HKSV Prebuffer: Ring Retention (seconds)",
+        "type": "integer",
+        "required": false,
+        "default": 15,
+        "minimum": 5,
+        "maximum": 60,
+        "description": "How much history each camera's in-memory ring holds. Must exceed the pre-roll, because the recording request arrives seconds AFTER the event timestamp it is anchored to. Costs roughly 150KB/s per camera (~2.3MB at the default)."
       }
     }
   }
